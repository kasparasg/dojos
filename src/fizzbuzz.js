module.exports = class FizzBuzz {
  constructor(min, max) {
    this.min = min;
    this.max = max;
  }

  run() {

  	for(let i = this.min; i <= this.max; i++) {
  		const message = 
  	}
  }

  isFizz(number) {
  	return !(number % 3);
  }

  isBuzz(number) {
  	return !(number % 5);
  }
};
